{% extends "base.html.twig" %}

{% block body %}

<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"> <img src="{{asset('photos/logo/logo all gen.png')}} " class="d-block w-25" alt="logo" width="250px"> </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="{{path('app_accueil')}}">Accueil
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_register')}}">Inscription</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_login')}}">Connexion</a>
        </li>
        {% else %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_logout') }}">Deconnexion</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_sujet')}}">Sujets</a>
        </li>
        {% endif %}
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_produits')}}">Articles</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_cart_commande')}}">Panier</a>
        </li>
        {% if is_granted('IS_AUTHENTICATED_FULLY') and is_granted('ROLE_ADMIN')%}
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Administration</a>
          <div class="dropdown-menu">
            <a class="dropdown-item" href="{{path('app_gestion_utilisateur')}}">Gestion des utilisateurs</a>
            <a class="dropdown-item" href="{{path('app_categories_add')}}">Création de categories</a>
            <a class="dropdown-item" href="{{path('app_produits_add')}}">Création Article</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="#">Separated link</a>
          </div>
        </li>
        {% endif %}
      </ul>
      <form class="d-flex">
        <input class="form-control me-sm-2" type="search" placeholder="Search">
        <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>

<main class="container h-100 mb-5 mt-5">
{% block content %} {% endblock %}
</main>


<footer class="fixed-bottom bg-dark">
		<div class="text-center text-white">
			<p>&copy; tous droits reservés, Mon forum</p>
    <a href="">Mentions légales</a> 
		</div>
	</footer>
















{% endblock %}